{
  "name": "blob-knight",
  "version": "1.1.0",
  "private": true,
  "description": "BLOB KNIGHT — six-level action RPG in vanilla JS + Canvas. Sword + ranged secondary + tools.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "vite": "^6.0.0"
  }
}